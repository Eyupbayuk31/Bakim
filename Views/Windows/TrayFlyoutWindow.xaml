<Window x:Class="Bakım.Views.Windows.TrayFlyoutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Sistem tepsisi" Height="600" Width="320"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        Deactivated="Window_Deactivated">
    
    <!-- Ana Çerçeve & Drop Shadow -->
    <Border Background="{DynamicResource ApplicationBackgroundBrush}"
            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="{StaticResource Radius.LG}"
            Margin="12">
        <Border.Effect>
            <DropShadowEffect BlurRadius="16" ShadowDepth="4" Opacity="0.45" Direction="270" Color="Black"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="Auto"/> <!-- Live Mini Telemetry -->
                <RowDefinition Height="Auto"/> <!-- Nöbetçi + son etkinlikler (§5.19) -->
                <RowDefinition Height="*"/>    <!-- Quick Actions -->
                <RowDefinition Height="Auto"/> <!-- Footer -->
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Border Grid.Row="0" Padding="16,12" BorderThickness="0,0,0,1" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <ui:SymbolIcon Symbol="ShieldTask24" FontSize="20" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Bakım sistem paneli" FontSize="{StaticResource Font.Body}" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                        <TextBlock x:Name="StatusText" Text="Sistem nöbette (Arka Plan Aktif)" FontSize="{StaticResource Font.Caption}" Margin="0,2,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Live Mini Telemetry -->
            <Border Grid.Row="1" Margin="14,12,14,0" Padding="12,10"
                    Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource Radius.MD}">
                <StackPanel>
                    <!-- CPU Row -->
                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ui:SymbolIcon Grid.Column="0" Symbol="DeveloperBoard20" FontSize="14" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="İşlemci (CPU)" FontSize="{StaticResource Font.Caption}" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" x:Name="CpuText" Text="%0" FontSize="{StaticResource Font.Caption}" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center"/>
                    </Grid>
                    <ProgressBar x:Name="CpuProgress" Height="4" Minimum="0" Maximum="100" Value="0" Margin="0,0,0,10" Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}"/>

                    <!-- RAM Row -->
                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ui:SymbolIcon Grid.Column="0" Symbol="TopSpeed20" FontSize="14" Foreground="{DynamicResource SystemFillColorSuccessBrush}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="Bellek (RAM)" FontSize="{StaticResource Font.Caption}" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" x:Name="RamText" Text="%0" FontSize="{StaticResource Font.Caption}" FontWeight="SemiBold" Foreground="{DynamicResource SystemFillColorSuccessBrush}" VerticalAlignment="Center"/>
                    </Grid>
                    <ProgressBar x:Name="RamProgress" Height="4" Minimum="0" Maximum="100" Value="0" Margin="0,0,0,4" Foreground="{DynamicResource SystemFillColorSuccessBrush}"/>
                    <TextBlock x:Name="RamDetailText" Text="Hesaplanıyor..." FontSize="{StaticResource Font.Caption}" Foreground="{DynamicResource TextFillColorTertiaryBrush}" HorizontalAlignment="Right"/>
                </StackPanel>
            </Border>
            
            <!-- Nöbetçi durumu ve son etkinlikler -->
            <Border Grid.Row="2" Margin="14,10,14,0" Padding="12,10"
                    Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource Radius.MD}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="ShieldCheckmark20" FontSize="14" Margin="0,0,6,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                        <TextBlock x:Name="SentinelText" Text="Kurulum Nöbetçisi" FontSize="{StaticResource Font.Caption}"
                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                    <TextBlock Text="Son etkinlikler" Style="{StaticResource Text.Label}" Margin="0,10,0,4"/>
                    <ItemsControl x:Name="RecentActivityList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" FontSize="{StaticResource Font.Caption}" TextTrimming="CharacterEllipsis"
                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,1"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <ui:Button Appearance="Transparent" Padding="0,4" Margin="0,4,0,0" Content="Etkinlik Merkezi'ni aç"
                               Click="BtnActivity_Click" FontSize="{StaticResource Font.Caption}"/>
                </StackPanel>
            </Border>

            <!-- Quick Actions -->
            <StackPanel Grid.Row="3" Margin="14,12">
                <ui:Button x:Name="BtnQuickBoost" Appearance="Primary" HorizontalAlignment="Stretch" Margin="0,0,0,6" Padding="10,8" Icon="{ui:SymbolIcon Broom24}" Content="Hızlı Bakım" ToolTip="Geçici dosyalar, tarayıcı önbellekleri ve DNS önbelleği (Temizleyici'deki güvenli kurallarla)" Click="BtnQuickBoost_Click"/>
                <ui:Button x:Name="BtnGameMode" Appearance="Secondary" HorizontalAlignment="Stretch" Margin="0,0,0,6" Padding="10,8" Icon="{ui:SymbolIcon Games24}" Content="Oyun Modunu başlat" Click="BtnGameMode_Click"/>
                <ui:Button x:Name="BtnShowMainWindow" Appearance="Secondary" HorizontalAlignment="Stretch" Margin="0,0,0,6" Padding="10,8" Icon="{ui:SymbolIcon Desktop24}" Content="Ana ekranı aç" Click="BtnShowMainWindow_Click"/>
                <ui:Button x:Name="BtnSettings" Appearance="Secondary" HorizontalAlignment="Stretch" Padding="10,8" Icon="{ui:SymbolIcon Settings24}" Content="Ayarlar" Click="BtnSettings_Click"/>
            </StackPanel>
            
            <!-- Footer -->
            <Border Grid.Row="4" Padding="14,10" Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" CornerRadius="0,0,12,12">
                <Grid>
                    <ui:Button x:Name="BtnExit" Appearance="Danger" HorizontalAlignment="Right" Padding="12,6" Icon="{ui:SymbolIcon Dismiss24}" Content="Çıkış" Click="BtnExit_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
